## 1. JWT -> Session 인증, 인가 방식 변경

JWT 기반 인증, 인가 방식에 대해 `왜 사용하는가?`에 대해서 생각해보았습니다. 이점은 아래와 같았습니다.

- HTTP 연결 시, 사용자 정보를 얻기 위해 인 메모리, DB 등 저장소 접근 불필요 -> `stateless server`
- 애플리케이션 내 서비스들을 모놀리식에서 모듈 단위 배포로 변경했을 때, 혹은 로드밸런싱을 진행할 때 인증 정보를 각 서버 내에서 완결지을 수 있음
- 클라이언트의 형태에 구애받지 않고, HTTP 통신의 `Authorization` 필드로 인증 정보를 담으면 끝

하지만 이번 미션에서 사용하는 형태는 위의 이점을 잘 살리지 못했습니다.

- 모놀리식 서비스
- 로그아웃 시, 토큰 만료 처리를 위해서 불가피한 `stateful server`
  - DB, Redis 등 추가 쿼리 필요
- 백엔드에서 직접 View를 서빙 중

때문에 JWT 인증 방식에서 세션 기반 인증 방식으로 수정하게 되었습니다.

## 2. Request Matcher 스타일 권한 검증 -> 컨트롤러 메소드 어노테이션 방식 변경

현재 미션에서 요구한 바는 아닙니다만, ID를 Path Variable로 받아 엔터티를 삭제하는 API를 일반 사용자가 호출할 수 있었습니다.

이는 사용자 측면에서 다른 사용자의 예약을 마음대로 삭제할 수 있는 문제가 있었으며, 현재 DB에서는 Member와의 관계로 현재 예약을 생성하는 것이 아니라 예약자 이름을 단순히 저장ㅎ
