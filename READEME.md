# Spring MVC [인증]

## 요구사항

- [x] 로그인 기능을 구현한다.
- [x] 로그인 성공 시 세션 id를 `token` 쿠키에 담아 응답한다.
- [x] 쿠키를 이용하여 로그인한 사용자의 정보를 조회한다.
- [x] 쿠키의 인증 정보로 회원 정보를 조회하는 로직을 분리한다.
- [x] `HandlerMethodArgumentResolver`를 이용하여 로그인 회원 정보를 컨트롤러에 주입한다.
- [ ] 예약 생성 시 이름이 없으면 로그인한 회원의 정보로 예약을 생성한다.
- [ ] 관리자 권한이 있는 회원만 어드민 페이지에 접근할 수 있다.
- [ ] `HandlerInterceptor`를 이용하여 권한을 확인하고, 권한이 없으면 상태 코드 `401`을 반환한다.

## 기능 목록

### 로그인

- [x] `POST /login` 요청으로 이메일과 비밀번호를 전달받는다.
- [x] 이메일과 비밀번호가 비어 있거나 공백만 포함되면 상태 코드 `400`과 검증 메시지를 반환한다.
- [x] 이메일 형식이 올바르지 않으면 상태 코드 `400`과 검증 메시지를 반환한다.
- [x] 이메일 또는 비밀번호가 일치하지 않으면 상태 코드 `401`과 동일한 로그인 실패 메시지를 반환한다.
- [x] 세션 id를 `token`이라는 이름의 쿠키에 담아 응답한다.
- [x] 로그인 실패 시 화면에서 오류 메시지를 alert로 표시하고 페이지를 이동하지 않는다.

### 인증 정보 조회

- [x] `GET /login/check` 요청으로 로그인한 사용자의 정보를 조회한다.
- [x] 요청의 `token` 쿠키에 담긴 세션 id로 기존 세션을 조회한다.
- [x] 세션이 없거나 세션에 회원 id가 없으면 상태 코드 `401`과 로그인 필요 메시지를 반환한다.
- [x] 조회한 회원을 응답 DTO로 변환하여 `name` 필드에 이름을 반환한다.
- [x] 쿠키에서 회원 정보를 조회하는 로직을 `LoginMemberArgumentResolver`로 분리한다.
- [x] 인증 정보 조회 API는 주입받은 로그인 회원 정보를 이용하여 이름을 응답한다.

### 로그인 상태 표시

- [x] 로그인 후 상단바 우측의 Login 버튼이 사용자 이름으로 표시된다.

### 예약 생성

- [ ] `POST /reservations` 요청으로 날짜, 시간대 id, 테마 id를 전달받는다.
- [ ] `ReservationRequest`에 `name`이 있으면 해당 이름으로 회원을 조회하여 예약을 생성한다.
- [ ] `ReservationRequest`에 `name`이 없으면 쿠키를 통해 주입받은 로그인 회원 정보로 예약을 생성한다.
- [ ] 요청에 `name`이 있으면 로그인한 회원의 이름보다 요청의 이름을 우선하여 예약자를 결정한다.

### 어드민 페이지 접근

- [ ] 컨트롤러에 진입하기 전에 `HandlerInterceptor`에서 쿠키의 인증 정보로 회원과 권한을 확인한다.
- [ ] 회원의 권한이 `ADMIN`이면 어드민 페이지 접근을 허용한다.
- [ ] 로그인한 회원이 없거나 `ADMIN` 권한이 없으면 상태 코드 `401`을 반환한다.

## 테스트 기준

### 로그인 화면

- [x] `GET /login` 요청 시 상태 코드 `200`과 로그인 HTML을 반환한다.

### 로그인

- [x] 올바른 이메일과 비밀번호로 `POST /login` 요청 시 상태 코드 `200`을 반환한다.
- [x] 응답의 `Set-Cookie` 헤더에 `token` 쿠키가 포함된다.

### 인증 정보 조회

- [x] 로그인 응답으로 받은 `token` 쿠키를 포함하여 `GET /login/check` 요청 시 상태 코드 `200`을 반환한다.
- [x] 응답의 `name` 필드에 로그인한 회원의 이름을 반환한다.

### 예약 생성

- [ ] 유효한 `token` 쿠키와 함께 `name` 없이 예약 생성 요청 시 상태 코드 `201`을 반환한다.
- [ ] `name` 없이 생성한 예약의 응답에는 로그인한 회원의 이름을 반환한다.
- [ ] 테스트 회원으로 로그인한 후 `name` 없이 예약하면 응답의 `name` 필드에 `어드민`을 반환한다.
- [ ] 유효한 `token` 쿠키와 함께 `name`을 포함하여 예약 생성 요청 시 상태 코드 `201`을 반환한다.

### 어드민 페이지 접근

- [ ] `ADMIN` 권한이 없는 회원이 `GET /admin` 요청 시 상태 코드 `401`을 반환한다.
- [ ] `ADMIN` 권한이 있는 회원이 `GET /admin` 요청 시 상태 코드 `200`을 반환한다.
- [ ] 로그인하지 않은 상태로 `GET /admin` 요청 시 상태 코드 `401`을 반환한다.
