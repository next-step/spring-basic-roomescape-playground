JWT 를 변환하고 파싱하는 것은 도메인인가? 도메인에 대한 생각을 정리하면서 해당 개념들을 정리하게 되었습니다.

# 문제점 병목지점
로그인 처리를 하는 로직이 UserService 안에 존재하게 되면서 문제가 발생했다. JWT를 하나의 비즈니스 규칙을 위한 도메인
이라고 생각이 들었고 해당 부분에 대해서 리뷰를 받고 생각을 정리하였는데, Jwt 가 도메인인가에 대해서 생각을 다시 하게되었다.

- JwtProvider 는 도메인이 아니라, 기술적인 도구일 뿐이다.
- 도메인은 비즈니스 규칙을 포함하여 해당 객체들이 어떻게 상호작용하고 데이터를 처리하는지를 포함한 개념이다.

이와 관련된 내용은 `review_1_extention.md` 파일에 정리해두었습니다.

간단하게 정리하면 `UserService` 에서는 도메인을 관리하고 비즈니스 규칙을 위해서 존재한다. 사용자의 데이터를 관리하는 것이다.
해당 역할과 인증에 대한 역할이 `UserService` 에서 함께 진행되고 있는 문제점이 있다.

로그인 처리를 하기 위해서는 request, response, session 등 변경에 유연하도록 authService 가 받아야하는 파라미터가
존재하는데, 이 부분을 UserService 에서 컨트롤러로부터 받아와 다시 authService에게 위임을 하게하는 것은 서비스의 
분리가 되지 않은 것이라고 생각이 들었다.

또한 기존의 UserService 의 메소드 login 안에서 추상화가 잘 안되는게 어떤 부분을 authService 에게 위임해야하는지 
구별해내는게 너무 어려웠다. 그래서 서비스를 분리하여 인증에 대한 부분을 분리해보자고 생각이 들었다.

authService 로 분리 (한 방식만을 구현한다고 가정하겠습니다.)

```java
@Service
public class AuthService {
    
    private final JwtProvider jwtProvider;
    
    public UserResponse login(HttpServletRequest request, HttpServletResponse response, String email, String password) {
        // 도메인 있는지 처리
        
        // jwt provider 로 토큰 생성
        
        // response 에 쿠키를 생성해서 토큰 값을 넣어줌
        
        // 찾은 유저 정보를 DTO 로 변환해서 리턴
    }
} 
```
해당 Jwt 로직이 있다고 가정하겠습니다. 만약 세션으로 바꾸게 된다면은 AuthService 를 인터페이스로 바꾸고 명세를 작성한 다음
SessionAuthService 로 구현체를 만들어서 다음과 같이 구현하거나 현재 서비스 클래스 파일을 변경하면 된다.
```java
@Service 
public class AuthService {
    public UserResponse login(HttpServletRequest request, HttpServletResponse response, String email, String password) {
        // 도메인 있는지 처리

        // request 에서 세션을 가지고온다.
        
        // 세션에 유저 정보를 키값으로 특정 정보를 담아준다. -> 세션이 등록되면 자동으로 set-cookie 에 JSESSIONID 에 담겨 반환됨

        // 찾은 유저 정보를 DTO 로 변환해서 리턴
    }
}

// 또는
@Service
public class SessionAuthService implements AuthService {
    
}
```

인증과 도메인의 로직이 분리되면서 책임이 나누어져 복잡도가 떨어졌다. 기존에 UserService 안에서 처리하려면 할 수 있다.

```java
public MemberResponse login(HttpServletRequest request, HttpServletResponse response, String email, String password) {
    // 데이터베이스에서 유저 정보를 가지고 온다.
    
    // 의존하고 있는 authService에게 멤버와 request, response 를 넘겨서 위임한다.
    
    // authService에서 쿠키 또는 세션 처리를 한다.
    
    // member 를 DTO 로 변환해서 리턴한다.
}
```
또한, 로그인한 유저인지를 확인하는 것은 UserService 와 분리되어야 하는 것도 맞는 것 같다. 어쩌다보니 먼저 생각을 하게
되었는데, 아마 미션을 계속 진행하다 이 부분에 대해서 리팩토링을 하지 않았을까? 하는 생각이 든다.
